# Contributing

`wp-dev` では、不具合報告と機能要望を GitHub Issues で受け付けています。

質問や一般的な使い方相談は主な受付対象ではありません。また、外部からの Pull Request は受け付けていません。

## 不具合報告

主なサポート対象は、このリポジトリが提供する構成で再現する `wp-dev` 自体の問題です。

対象には、たとえば次のような問題が含まれます。

- `wp-dev` が提供する Dev Container / Docker Compose 環境の不具合
- README やリポジトリ内ドキュメントの手順どおりに利用して発生する問題
- リポジトリが提供する環境設定テンプレートを利用して再現する問題
- `wp-dev` のスクリプト、設定、WordPress 開発環境そのものに原因がある問題

次のような問題は、原則として主なサポート対象外です。

- 開発対象としてマウントした個別の WordPress プラグイン / テーマの不具合
- 利用者が独自に大きく変更した Docker / Dev Container 構成
- `wp-dev` が提供していない WordPress / PHP / Node.js 等の組み合わせ固有の問題
- Docker、WordPress、Visual Studio Code など一般製品そのものの使い方や不具合
- 個別環境への継続的なトラブルシューティングやサポート保証

対象外に該当する可能性がある環境からの報告も一律には拒否しませんが、内容を確認した結果、`wp-dev` 側では対応できない場合があります。

不具合報告では、可能な範囲で次の情報を含めてください。

- 再現手順
- 期待した結果
- 実際に起きた結果
- 使用した `wp-dev` の環境（`default` / `wp683` など）
- ホスト OS
- 関連するエラーやログ

ログや設定を共有する場合は、パスワード、トークン、その他の機密情報が含まれていないことを確認してください。

## 機能要望

機能要望は GitHub Issues で受け付けています。ただし、提案された機能の実装は保証しません。

具体的な実装方法だけでなく、「何に困っているか」「どのような用途か」が分かるように記載してください。

## 質問・使い方相談

質問や一般的な使い方相談は GitHub Issues の主な受付対象ではありません。Docker、WordPress、Visual Studio Code など外部製品の一般的な使い方については、それぞれの公式ドキュメントを参照してください。

ただし、質問や相談の内容から `wp-dev` 自体の不具合、ドキュメント不足、改善すべき点が判明した場合は、不具合報告または機能要望として Issue を作成できます。

## Pull Request

外部からの Pull Request は受け付けていません。不具合や改善の提案は GitHub Issues からお知らせください。

リポジトリ内部の開発では、引き続き Pull Request を利用します。

## セキュリティ上の問題

セキュリティ上の問題は公開 Issue に投稿しないでください。報告方法は [`SECURITY.md`](SECURITY.md) を参照してください。
